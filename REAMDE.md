# mink-frontend-sb

Språkbanken instance config for [Mink frontend](https://github.com/spraakbanken/mink/) (the `instance` branch which is currently still under development).

Clone it next to `mink-frontend` and create a symbolic link from `mink-frontend/instance` to `mink-frontend-sb`.
